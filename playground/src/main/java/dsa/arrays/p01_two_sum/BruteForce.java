package dsa.arrays.p01_two_sum;

/**
 * 1. Two Sum — Brute Force (skipped — optional)
 *
 * LeetCode: https://leetcode.com/problems/two-sum/
 * Note:     "Arrays/1. Two Sum.md" (repo root)
 *
 * Your first attempt already landed on the optimal hash-map approach (now
 * in Optimized.java) — see AGENTS.md's "Exception: their first attempt is
 * already optimal". Implementing the true naive version below is optional,
 * not required to move on.
 *
 * For reference, the naive approach: check every pair (i, j) directly with
 * nested loops, no extra memory.
 *
 * Time:  O(n^2) — every pair checked
 * Space: O(1) — no extra structures
 */
public class BruteForce {

    public static int[] twoSum(int[] nums, int target) {
        // Optional: implement the nested-loop version here if you want the
        // side-by-side comparison. Not required — Optimized.java is the
        // solution of record for this problem.
        throw new UnsupportedOperationException("skipped — see class-level comment");
    }
}
